name: Release pull request review

on:
  pull_request_review:
    types:
      - submitted
    branches:
      - 'release-*'

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - run: |
          echo "${{ toJson(github) }}"

      - run: |
          cargo publish --dry-run
